# 피그말리온 프로덕트 엔지니어 과제 — 기획 문서

## 1. 과제 재해석

과제는 "가장 재미있을 AI 연인 서비스를 프로토타입으로 완성하라"지만, 출제 의도를 보면 평가축은 네 가지다.

1. AI 로맨스 도메인에 대한 이해와 감수성
2. 기한 내 완성을 위해 무엇을 포기할지 판단하는 능력
3. 낯선 스택도 AI로 빠르게 학습해 붙는 능력
4. AI로 빠르게 짠 코드의 복잡성과 리스크를 관리하는 능력

즉 "얼마나 화려하게 만들었나"보다 "어떤 재미를 무엇으로 증명했고, 무엇을 왜 포기했는가"가 핵심이다.

## 2. 컨셉: "대화를 끝낸 뒤에도 관계가 계속되는 AI 연인"

초기 버전의 컨셉은 "읽씹하면 삐지고, 은근히 질투하고, 사소한 걸 기억하는 연인"이었다. 재미있는 표현이지만
기능을 나열한 것에 가까웠다. 다시 정리하면 이 프로토타입이 검증하려는 건 하나의 제품 가설이다.

> **사용자는 AI가 현재 메시지에 잘 답할 때보다, 지나간 시간과 이전 대화, 현재 관계와 감정을 이어서
> 행동할 때 "이 AI가 실제로 나를 신경 쓰고 있다"는 감정을 더 강하게 느낀다.**

Presence / Hidden Emotion / Memory / Event는 각각 독립 기능이 아니라, 이 가설 하나를 증명하는 네 개의 장치다.

- **Presence** — 내가 없던 시간도 관계에 영향을 준다.
- **Hidden Emotion** — 캐릭터가 감정을 직접 설명하지 않고 말투와 행동으로 보여준다.
- **Memory** — 이전 대화가 단순 회상이 아니라 현재 행동의 원인이 된다.
- **Event** — 삭제 메시지, 시간 점프, 재접속 시 먼저 말 걸기 같은 "말이 아닌 행동"으로 감정을 표현한다.

### 2-1. Presence — 경과 시간 + 직전 대화 분위기 + 관계 단계

순수 경과 시간 임계값(`lib/mood.ts`의 `baseStateFromElapsed`)이 1차 기준이지만, 같은 침묵도 맥락에 따라
다르게 해석되도록 최소한의 규칙을 얹었다.

| 규칙 | 조건 | 효과 |
|---|---|---|
| 기본 사다리 | 경과 시간 | ~30분 평온 / 30분~3h 그리움 / 3h~12h 서운함 / 12h+ 삐짐 |
| 규칙 1 | 직전 대화가 `warm`(다정) + 6시간↑ | 서운함 대신 그리움으로 완화 |
| 규칙 2 | 직전 대화가 `conflict`(질투/서운/어색) + 3시간↑ | "어색함(awkward)"으로 전환 — 화해도 안 됐고 삐진 것도 아닌 애매한 톤 |
| 규칙 3 | 관계 단계가 아직 "오래된 친구"(초반) | 세게 삐지는 대신 조심스럽게 먼저 말 거는 쪽으로 완화 |

`lastConversationMood`는 직전 턴의 `emotion`(affectionate→warm, jealous/hurt/awkward→conflict, 그 외
neutral)에서 매 턴 파생되어 세션에 저장된다.

### 2-2. Hidden Emotion — Jealousy를 감정 상태 시스템으로 일반화

질투만 별도 기능으로 두지 않고 6개 감정 enum(`neutral / missing / jealous / hurt / affectionate / awkward`)
으로 일반화했다. 매 턴 LLM이 구조화 출력으로 이번 턴의 `emotion`/`intensity`를 판단하고, 이 값이 세션에
이어져(`lib/jealousy.ts`의 `buildEmotionPromptHint`) 다음 턴 캐릭터의 말투에 연속성을 부여한다.

캐릭터는 감정을 절대 말로 설명하지 않는다.

- 나쁜 예: "나 질투나."
- 좋은 예: "둘이?" → (유저가 대수롭지 않게 답하면) "아니 그냥." → "재밌게 보고 와."

키워드 기반 감지(`JEALOUSY_KEYWORDS`)는 더 이상 감정을 켜고 끄는 게이트가 아니라, LLM 판단을 보조하는
참고 신호로만 프롬프트에 살짝 얹는다. 최종 판단은 항상 LLM의 구조화 출력이 담당한다.

### 2-3. Memory — 회상이 아니라 감정으로 연결

기억의 목적은 "과거 정보를 정확히 재현하는 것"이 아니라 "과거 대화를 현재의 행동과 감정으로 연결하는
것"이다. 유저가 "그 영화 보고 싶었는데"라고 말한 걸 저장해뒀다가, 나중에 질투 상황에서 단순 회상
("전에 그 영화 얘기했었잖아")에서 끝내지 않고 지금 감정과 연결한다("나도 같이 보고 싶었는데").

```
Memory → Current Emotion → New Story/Event
```

`lib/memory.ts`의 키워드 겹침 + 최근성 스코어링은 그대로 유지하고, `buildMemoryPromptHint`의 문구만
"회상"에서 "연결"로 바꿨다.

### 2-4. Event — 대사가 아니라 행동

말로 설명하는 챗봇에서 끝나지 않기 위해, 최소 2종 이상의 "행동" 장치를 넣었다 (`lib/events.ts`,
`app/api/chat/route.ts`).

1. **메시지 삭제 (deleted_message)** — 매 턴 구조화 출력의 `event` 필드로 LLM이 직접 판단한다. 감정을
   못 참고 티를 낸 직후처럼 정말 그럴 법한 순간에만 발생하고, 유저가 "뭐 지웠어?"라고 물으면 다음 턴
   시스템 프롬프트에 접힌 힌트(`recentDeletedMessageHint`)가 얼버무리는 반응을 유도한다.
2. **재접속 시 먼저 말 걸기 (reconnect_first_message)** — 세션 재진입(`GET /api/chat`) 시 Presence
   상태가 평온이 아니면, 유저 발화 없이 캐릭터가 먼저 말을 건다. "관계가 이어진다"는 가설과 가장
   직접적으로 맞닿아 있는 장치.
3. **시간 점프 (time_skip)** — LLM 호출 없이 순수 계산으로 "N시간 후" 구분선을 삽입한다. 위 두 이벤트에
   비해 거의 비용이 들지 않아 함께 구현했다.

한 번 이벤트가 발생하면 `last_active_at`이 갱신되므로, 같은 침묵 구간에서 중복 발생하지 않는다.

### 데모 에피소드 (하나의 감정선으로 통합)

기능을 각각 보여주기보다, 하나의 짧은 에피소드 안에서 Presence·Hidden Emotion·Memory·Event가 모두
드러나게 설계했다.

- **캐릭터**: 10년지기 남사친 "이준" — 서로 친하지만 아직 연애 감정을 말로 꺼낸 적 없는 사이
- **Scene 1**: 유저 "내일 회사 동기랑 영화 보기로 했어ㅋㅋ" → "둘이?" → "아니 그냥." → (메시지 삭제 이벤트)
- **Scene 2**: 유저가 몇 시간 자리를 비움 → 재접속 시 "N시간 후" 카드 + 캐릭터가 먼저, 평소보다 건조한
  톤으로 말을 건다 ("...왔어?")
- **Scene 3**: 유저가 다정하게 풀어주면 감정이 완화되고, 캐릭터가 예전에 유저가 "보고 싶다"고 했던
  영화 얘기를 지금 감정과 연결해서 꺼낸다 ("그거 나도 보고싶었던 거 아냐?")
- **관계 단계**: "오래된 친구" → "요즘 유독 편해진 친구" → "친구라고 하기엔 조금 이상한 사이" (숫자는
  내부에만 존재, UI에는 텍스트로만 노출)

## 3. 아키텍처

```
Browser (app/page.tsx)
  - localStorage에는 session_id만 저장, 모든 요청에 X-Session-Id 헤더로 전달
      │
      ▼
Next.js API Routes (app/api/chat/route.ts) — 서버 전용
  - GET  세션 부트스트랩/로드 + Presence 계산 + (조건 충족 시) 재접속 먼저 말걸기
  - POST Presence/기억 계산 + 시스템 프롬프트 조립 + 구조화 LLM 호출 1회 + Zod 검증 + 반영
      │                                   │
      │ Supabase (service_role, RLS       │ Anthropic API (claude-sonnet-5)
      │ deny-all, 브라우저 직접 접근 없음)   │ tool_choice로 강제한 JSON 1개:
      ▼                                   │ {message, emotion, intensity,
  sessions / messages / memories          │  relationshipDelta, memory, event}
```

**세션**: 로그인 없이 `X-Session-Id` 헤더 + `localStorage`만으로 식별하는 익명 세션. 헤더가 없거나
DB에 없으면 새 세션을 만들고(기존 데이터 덮어쓰지 않음), 조회 자체가 실패(네트워크 장애)하면 새 세션을
만들지 않고 에러만 반환해 클라이언트가 기존 `localStorage` 값을 잃지 않게 한다.

**LLM 호출**: SDK 없이 기존 `fetch` 패턴을 유지하고, Anthropic의 `tools` + 강제 `tool_choice`로 JSON
스키마를 강제한다. 대사 생성 + 감정/관계/기억/이벤트 판단(개념적으로는 Director/Actor)을 한 번의 호출로
합쳐서 latency·비용·실패 지점·구현 복잡도를 줄였다. 응답은 항상 Zod(`lib/schema.ts`)로 검증한 뒤에만
세션에 반영하고, 검증 실패나 빈 응답은 예외로 처리해 세션을 오염시키지 않는다.

## 4. Supabase 스키마

`db/migrations/0001_init.sql` 참고. 3테이블 구조.

- **sessions**: `id, relationship_stage, relationship_score, emotion, emotion_intensity, last_conversation_mood, last_active_at, created_at, updated_at`
- **messages**: `id, session_id, role(user/assistant/system_event), content, event_type(deleted_message/time_skip/reconnect_first_message), created_at`
- **memories**: `id, session_id, content, importance, created_at, last_used_at`

RLS는 켜두고 정책은 만들지 않는다(deny-all). 서버는 오직 `service_role` 키로만 접근하며 이 키는 RLS를
우회하므로, `anon` 키는 이 프로젝트에서 아예 생성/노출하지 않는다.

## 5. 기능 우선순위 (MoSCoW)

**Must**
- 1:1 자유 채팅, 명확한 캐릭터 페르소나, LLM 연동
- 경과 시간 + 직전 대화 + 관계 단계 기반 Presence
- Hidden Emotion(감정을 말하지 않고 표현) + LLM structured output + Zod validation
- 기억 추출 및 이후 대화에서 감정과 연결해 활용
- 이벤트 최소 2종(메시지 삭제, 재접속 먼저 말걸기) + 시간 점프
- Supabase 기반 익명 세션, 대화/기억/관계 상태 영속화, 재접속 시 복원
- 5~10분 안에 플레이 가능한 하나의 에피소드

**Should**
- 타이핑 인디케이터(답장 길이에 비례한 지연), 시간 표시
- 관계 상태를 텍스트로 표현(숫자 비노출)
- 기본적인 네트워크 오류 처리(세션 로드 실패 인라인 에러)
- 탭을 열어둔 채 기다리면 캐릭터가 먼저 말 거는 연출(주기적 폴링)
- 브라우저 푸시 알림 — 탭/브라우저를 닫아도 캐릭터가 먼저 연락하면 실제 OS 알림으로 전달
  (당초 Won't였으나, 데모를 다듬는 과정에서 "칼같이 즉답하는 챗봇처럼 느껴진다"는 피드백을 받고
  우선순위를 올려 구현 — 아래 6장 참고)

**Could**
- 추가 이벤트(입력하다 멈춤), 추가 관계 시나리오, 페르소나 프리셋, 세션 초기화 버튼, 여러 기기 이어하기

**Won't**
- 관리형 스케줄러 인프라(Vercel Cron 등 실제 배포) — 로컬 개발에서는 대체용 스크립트로 검증만 함
- 회원가입/로그인, 여러 캐릭터, 음성 AI, 이미지 생성, 벡터DB, 정교한 장기 기억 시스템, React Native,
  복잡한 인프라

## 6. 트레이드오프 / 리스크 관리 메모

- **스택**: Django + Expo가 아닌 Next.js 단일 스택. 3~5일 안에 "재미"를 증명하는 게 목표이지 스택
  재현이 목표가 아니기 때문. 도메인 로직(`lib/`)은 프레임워크 비의존적으로 분리해 이식 가능하게 유지.
- **"실제 푸시 알림"을 Won't에서 Should로 올린 이유**: 초기엔 재접속 시 경과 시간을 계산해 캐릭터가
  먼저 말 거는 것으로 충분히 시뮬레이션된다고 판단했다. 하지만 실제로 써보니 매 답장이 즉각적으로
  오는 것 자체가 "AI 같다"는 인상을 줬고, 재접속 연출도 결국 유저가 다시 탭을 열어야만 발동돼서
  "관계가 알아서 이어진다"는 느낌이 약했다. 그래서 (1) 답장 길이에 비례한 타이핑 지연, (2) 탭을
  열어둔 채로도 먼저 말 거는 폴링, (3) 탭/브라우저를 닫아도 오는 실제 Web Push까지 추가했다.
  다만 진짜 스케줄러(Vercel Cron 등) 배포는 범위 밖으로 남겨두고, 로컬 트리거 스크립트로 검증만
  했다 — "무엇을 실제로 만들고 무엇은 로컬 검증까지만 하는지"를 가르는 이번 판단 자체가 하나의
  트레이드오프다.
- **저장소를 파일 → Supabase로 전환한 이유**: 초기에는 `localStorage`/파일 저장만으로 상태를 유지하는
  방안도 검토했다. 하지만 재접속 후에도 이전 대화·감정·기억이 이어지는 경험 자체가 이번 제품 컨셉
  ("대화가 끝나도 관계는 계속된다")과 직접 연결된다고 판단해, 최소한의 서버 영속 저장소를 도입했다.
  반면 사용자 인증은 핵심 가설과 직접 관련이 없어 익명 세션 방식으로 제한했다.
- **감정 감지를 키워드 → LLM structured output으로 바꾼 이유**: 키워드 매칭은 오탐이 많고(예: "동생이랑
  밥 먹었어"도 걸릴 수 있음), AI를 제품 로직에 활용하는 회사의 과제 취지와도 맞지 않는다고 판단했다.
  다만 LLM 출력을 무조건 신뢰하지 않고, 허용된 enum/범위로 제한한 스키마 + Zod 검증을 항상 거친다.
- **LLM 호출을 1회로 합친 이유**: Director(감정/관계/이벤트/기억 판단)와 Actor(대사 생성)를 논리적으로는
  분리하되, 실제 호출은 하나로 합쳤다. latency, API 비용, 실패 지점, 구현 복잡도를 모두 줄이는
  타협이었다.
- **기억 시스템**: 정교한 임베딩 검색 대신 키워드 겹침 + 최근성 정도의 단순 규칙. 지금 단계에서
  보여줘야 할 건 "기억이 감정과 연결되는 경험이 재미있는가"이지 검색 정확도가 아니다.
- **인증**: 회원가입/로그인/OAuth를 만들지 않았다. 이 기능들은 핵심 재미를 증명하는 데 직접적인 영향을
  주지 않는다고 판단했다. 대신 익명 `session_id` + service_role 전용 접근으로 최소한의 접근 제어는
  유지한다.

## 7. AI를 활용한 개발 과정

**AI 활용**: UI scaffold, 반복 컴포넌트, API route 초안, Supabase schema/쿼리 초안, structured output
스키마 초안, 프롬프트 반복, 디버깅(예: Anthropic API가 간헐적으로 빈 텍스트를 반환하는 문제, LLM
history 직렬화 시 여분 필드가 섞여 API가 거부한 문제 등을 실제 대화로 재현하며 원인을 좁혀 수정).

**코드 복잡도 관리 원칙**
- AI가 제안한 추상화를 그대로 도입하지 않는다 (예: 구조화 출력에 SDK를 새로 추가하자는 제안 대신,
  기존 `fetch` + `tool_choice` 패턴을 유지해 의존성을 최소화했다).
- 현재 MVP에서 실제로 필요한 구조만 유지하고, 두 번 이상 반복되지 않는 로직은 성급하게 공통화하지 않는다.
- LLM 응답과 애플리케이션 상태 사이에 항상 Zod 검증 경계를 둔다 (`lib/schema.ts` → `lib/llm.ts`).
- 감정 상태 변경 로직(`lib/mood.ts`, `lib/jealousy.ts`)과 UI 표현(`app/page.tsx`)을 분리한다.
- Supabase 접근 로직(`lib/store.ts`, `lib/supabase.ts`)을 UI 컴포넌트와 직접 결합하지 않는다.

## 8. 주요 리스크와 대응

| 리스크 | 대응 |
|---|---|
| LLM 응답 포맷 실패 | Zod 검증, 실패 시 세션에 반영하지 않고 에러 반환, 허용 enum/범위 제한 |
| 감정을 너무 직접적으로 말함 | 시스템 프롬프트에서 직접 설명 금지 + few-shot 예시(좋은 예/나쁜 예) 제공 |
| 지나친 집착/반복적 삐짐 | intensity 상한(0~1), 관계 초반 단계는 강한 삐짐 대신 완화 규칙 적용 |
| 중복 API 요청 | 전송 중 입력 잠금(`loading` 상태로 버튼/전송 비활성화) |
| 관련 없는 순간에 기억이 튀어나옴 | 키워드 겹침 + 최근성 스코어링, 점수 0 이하는 사용 안 함 |
| Supabase 저장 실패 | 세션 조회 "없음"과 "실패"를 구분, 실패 시 새 세션으로 덮어쓰지 않고 인라인 에러만 표시 |
| 세션 ID 노출/무단 접근 | RLS 켜두고 정책 없음(deny-all), 서버는 service_role만 사용, anon 키 미사용 |

## 9. 최종 제품 메시지

이 프로토타입은 "AI가 얼마나 말을 잘하는가"를 증명하려는 게 아니다.

> 대화를 하지 않는 시간에도 관계가 이어지고, 캐릭터가 말하지 않은 감정을 사용자가 스스로 해석하게
> 된다면 AI가 조금 더 '신경 쓰이는 사람'처럼 느껴질 수 있을까?

그리고 이전 대화와 감정이 실제로 저장되어, 창을 닫았다 다시 들어와도 관계가 이어지는 것까지 이
경험의 일부로 본다. 최종 성공 기준은 기능 개수가 아니라 — **5~10분 플레이한 사용자가 캐릭터의 다음
말이나 행동이 궁금해지는가** — 이다.
